# Contributor: artist for XLibre

pkgname=xlibre-xf86-video-r128
_pkgname=xf86-video-r128
pkgver=6.13.0.2
pkgrel=1
pkgdesc="XLibre fork of X.org r128 video driver"
arch=(x86_64 aarch64)
url="https://github.com/X11Libre"
license=('custom')
depends=('glibc')
makedepends=('xlibre-xserver' 'xlibre-xserver-common' 'xlibre-xserver-devel' 'X-ABI-VIDEODRV_VERSION=28.0')
groups=('xlibre-drivers')
source=("${url}/${_pkgname}/archive/refs/tags/${pkgname}-${pkgver}.tar.gz")
sha256sums=('1e5e6ad110da7e51cdf9d44391c1a55de29106b7b95420a6e1b9d6234a60e323')

build() {
  cd ${_pkgname}-${pkgname}-${pkgver}
  ./autogen.sh
  ./configure --prefix=/usr
  make
}

package() {
  cd ${_pkgname}-${pkgname}-${pkgver}
  make DESTDIR="${pkgdir}" install
  install -m755 -d "${pkgdir}/usr/share/licenses/${pkgname}"
  install -m644 COPYING "${pkgdir}/usr/share/licenses/${pkgname}/"
}
